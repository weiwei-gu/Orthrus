# TODO — 后续方向

> 依据：DR 终局判决（`results/dr_finetune_verdict.json`，2026-09-20）、
> 技术文档 §3.5/§5.3/§6（`docs/index.html`）。负结果与判决数据都已入档，此处只列"下一步做什么"。

## P0 · sim2sim 缺口（推力段 18 检查点转移全灭的三个解法）

- [ ] **接触参数随机化**：把 solref/solimp 加入 DR 分布（当前只随机摩擦/质量/阻尼/电机——
      判决证明物理参数 DR 关得掉行走漂移、关不掉推力段死亡，缺口在接触求解器层）
- [ ] **部署侧接触适配**：反向思路——把 MuJoCo 部署场景的接触参数对齐 MJX 训练分布，复测 s3/s4
- [ ] **推力训练加 MuJoCo 快照准入门槛**：训练链每个评估点先过 MuJoCo sim2sim 行走测试，
      不过线就回滚上一个快照（把双引擎验证从"事后判决"变成"训练回路内约束"）

## P1 · 部署管线技术债（文档 §3.5 如实入档，实测可容忍但未修）

- [ ] **Mac GPU 路线已实测排除**（`docs/jax-metal-verdict.md`）：jax-metal 缺 mhlo.cholesky kernel、
      闭源无法自行维护——RL 训练的 GPU 加速**只剩云 CUDA**（RTX 4090 约 ¥2/h，DR 链 2h10m → 预计 10~20min）
- [ ] **部署端补观察归一化**：`eval_go2_rl.py`/混合控制器构造网络时 preprocess 应为
      `rs.normalize`（当前恒等，喂原始观察；实测 raw/normalized 两路动作差 mean|Δa|≈0.17）。
      修复后**必须复测** bc_init 与混合的全套件——当前 60%/94% 都对应 raw 路径
- [ ] **策略频率统一**：训练 8 ms（N_FRAMES=4 × timestep 0.002 = 125 Hz）vs 部署 20 ms（50 Hz）；
      时钟在观察内所以步态不受影响，但控制带宽不一致，择一对齐

## P2 · 性能边界

- [x] **架构内优化方向已全部穷尽（2026-09-26 战役，同一把 72 试验尺子）**：模型保真度（NMPC 40%）、
      扰动前馈（DOB 35%）、组合（NMPC+DOB 40%，SCvx 吸收噪声的涌现交互但零收益）、
      捕获点落脚重规划（CP 24%，位置捕获在髋参考步进里结构免费）——全部无收益或负收益。
      40% (MPC) / 94% (混合) 为当前架构实测硬墙；混合版 4 个 30N·s 侧向失败经取证均摔在
      RL 救援期内（roll 全 RL 控制下发散至 124°，无接回）= bc_init 侧向极限
- [ ] **唯一剩余杠杆是跨架构的**：①P0 的 sim2sim 修复（接触参数 DR / MuJoCo 准入回路）——
      让带推力训练的救援脑活下来，替代 bc_init 的侧向短板；②或脚本化侧向反射步序列
      （两步宽站捕获，作为仲裁器第三模式）
- [ ] **30 N·s 纯侧向 4 个失败**：捕获点超出单步工作空间，需多步恢复规划（连续捕获步序列）
- [ ] **混合版行走 0.3 → 0.5 m/s**：hybrid 场景足端半径较小（0.0175 vs 0.022），MPC 步态参数待适配
- [ ] **DR s2 甜点的 Pareto 探索**：13.76M（+0.17 m/s）套件仅 35%——试推力课程从 bc_init
      起跑（而非 s2 终点），或对 13.76M 用更小 lr 做短程推力精调，寻找"快且禁踹"的点

## P3 · 走向真机

- [ ] IMU 噪声模型 + 状态估计器（当前状态全用仿真真值）
- [ ] 足端力传感缺失下的接触检测（当前用真值接触）
- [ ] 技能扩展（挥手/作揖）：MPC+IK 脚本化，或 RL 技能配方训练
- [ ] 真机部署最小闭环（哪怕只到"站立不倒"）
